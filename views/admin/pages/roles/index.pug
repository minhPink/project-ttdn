extends ../../layouts/default.pug 

block main 
    h1 Nhóm quyền

    .card.mb-3 
        .card-header Danh sách
            .card-body
                .row
                    .col-8 
                    .col-4
                        a(
                            href=`${prefixAdmin}/roles/create`
                            class= "btn btn-outline-success mb-3"
                            ) + Thêm mới

                table(
                    class="table table-hover table-sm table-bordered"
                    checkbox-multi
                )
                    thead
                        tr  
                            th.text-center.align-middle STT 
                            th.text-center.align-middle Nhóm quyền 
                            th.text-center.align-middle Mô tả ngắn
                            th.text-center.align-middle Hành động
                    tbody
                        each item, index in records
                            tr
                                td.text-center.align-middle #{index + 1}
                                td.text-center.align-middle #{item.title}
                                td.text-center.align-middle #{item.description}
                                td.text-center.align-middle 
                                    a(  
                                        href=`${prefixAdmin}/roles/detail/${item.id}`
                                        class="btn btn-secondary btn-sm"
                                        type='button'
                                    ) Chi tiết
                                    a(  
                                        href=`${prefixAdmin}/roles/edit/${item.id}`
                                        class="btn btn-danger btn-sm"
                                        type='button'
                                    ) Sửa
                                    button(
                                            class="btn btn-warning btn-sm ml-1"
                                            data-id=item.id
                                            button-delete
                                        ) Xóa



    script(src="/admin/js/products.js")
            
                     
